# 产品规格

## 问题

Agent 工具 Schema 可能通过本地类型检查，却因名称、描述、参数封装或对象约束差异被服务商拒绝。本工具在发布 MCP 或工具调用前提供快速、可复现的预检。

## 范围

- 读取一个 JSON 工具定义；
- 检查通用必填字段和服务商特定封装；
- 输出稳定规则 ID，支持文本、JSON、SARIF；
- 不调用模型，不发送网络请求。

## 非目标

不做 Schema 转换、远程验证，也不替代服务商 SDK。
